# Erion Ember v3 — Environment Configuration
# cp .env.example .env

# ─── Server ──────────────────────────────────────────────
HTTP_PORT=8080

# ─── Cache ───────────────────────────────────────────────
# SimHash similarity threshold [0.0–1.0]
# 0.85 → max 9 differing bits out of 64 (1-(0.85*64)≈9)
CACHE_SIMILARITY_THRESHOLD=0.85

# Maximum cached entries (LRU eviction when exceeded)
CACHE_MAX_ELEMENTS=100000

# Default TTL in seconds (0 = no expiry)
CACHE_DEFAULT_TTL=3600
